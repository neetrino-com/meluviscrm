# План на сегодня: Реализация MD5 для файлов

## 📋 Краткое описание задачи

**Проблема:** Сторонняя команда каждый раз скачивает все файлы через API, создавая лишнюю нагрузку на сервер.

**Решение:** Добавить MD5 хеши для файлов, чтобы клиент мог проверить, какие файлы у него уже есть, и скачать только новые.

## 🔍 Что такое MD5?

MD5 — это уникальный "отпечаток" файла (32 символа). Для одного и того же файла всегда получается одинаковый MD5. Это позволяет:
- Проверить, изменился ли файл
- Избежать повторных загрузок одинаковых файлов
- Экономить трафик и нагрузку на сервер

## ✅ План реализации (6.5 дней)

1. **Обновление БД** (1 день) — добавить поле `md5Hash` в таблицу `apartment_attachments`
2. **Вычисление MD5 при загрузке** (1 день) — автоматически вычислять MD5 для новых файлов
3. **Миграция существующих файлов** (1 день) — скрипт для вычисления MD5 для старых файлов
4. **Добавление MD5 в API** (1 день) — возвращать MD5 в ответах API
5. **Новый endpoint для проверки** (1 день, опционально) — endpoint для массовой проверки файлов
6. **Документация** (0.5 дня) — обновить API спецификацию
7. **Тестирование** (1 день) — протестировать все функции

## 📄 Подробный план

См. файл: `Documents/MD5-IMPLEMENTATION-PLAN.md`

## 🎯 Что нужно сделать

1. Добавить поле `md5Hash` в Prisma схему
2. Создать миграцию БД
3. Модифицировать код загрузки файлов для вычисления MD5
4. Создать скрипт миграции для существующих файлов
5. Обновить API endpoints для возврата MD5
6. Обновить документацию
